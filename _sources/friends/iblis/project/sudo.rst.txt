sudo
===============================================================================

Some distro, like ``manjaro``, override the rule via ``/etc/sudoers.d/*``.
So changing the config via ``visudo`` may not work.
